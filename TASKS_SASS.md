# ТЗ

## Завдання: Рефакторинг коду проекту WebStudio

## Стартові налаштування проекту та файли:

Для організації стилей використовується модульний підхід та синтаксис `@use`:

- У файлі `main.scss` реалізована логіка підключення паршалів (**partials**)
- 📁 Структура Проекту

Проект має наступну організацію файлів:

```text
.
├── hw-07/                        # Основний код додатка (Source Code)
├── index.html                    # Головна сторінка
├── sass/                         # Стилі проекту
│   ├── main.scss                 # Головний файл з імпортами
│   ├── base                      # Папка з базовими стилями
│   │   ├── _common.scss          # Стилі із загальними налаштувнанями
│   │   ├── _normalize.scss       # Стилі для нормалізацї елементів
│   │   ├── _reset.scss           # Стилі для ресету елементів
│   │   └── _container.scss       # Стилі контейнера сторінки
│   ├── layout                    # Папка основних частин сайту
│   │   ├── _header.scss          # Стилі секції хедер
│   │   ├── _nav.scss             # Стилі блоку навігації
│   │   ├── _footer.scss          # Стилі секції футер
│   │   └── _forms.scss           # Стилі форм сайту
│   ├── components                # Папка з компонентами
│   │   ├── _hero.scss            # Стилі секції герой
│   │   ├── _benefits.scss        # Стилі секції переваги
│   │   ├── _team.scss            # Стилі секції команда
│   │   ├── _portfolio.scss       # Стилі секції портфоліо
│   │   ├── _modal.scss           # Стилі модального вікна
│   │   ├── _mobile-menu.scss     # Стилі мобільного меню
│   │   └── _socials.scss         # Стилі іконок соціальних мереж
│   └── helpers                   # Папка з іструументами та хелперами
│       ├── _variables.scss       # Sass змінні
│       ├── _mixins.scss          # Sass міксини
│       ├── _functions.scss       # Sass функції
│       └── _placeholders.scss    # Sass плейсхолдери
├── images/                       # Файли зображень
├── .prettierrc.json              # Приклад файлу для форматування проекту
├── .gitignore                    # Файли, що ігноруються Git
└── README.md                     # Ви читаєте цей файл
```

- У файлі cart.js логіка сторінки Cart (cart.html)
- У файлі products-api.js зберігай функції для запитів на бекенд. У файлі
  render-functions.js зберігай функції для відображення елементів інтерфейсу.
